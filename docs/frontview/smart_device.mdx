# Настройка FrontView у умных устройств

Умные устройства имеют уникальный интерфейс, что позволяет не заполнять поле **keys** во frontview.

Поэтому у умных устройств мы только заполняем категорию устройства

```json
"config": {
    "category": "teremok"
}
```

